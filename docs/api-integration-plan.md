# C-PC 真实接口接入与承接计划

> 本文只维护当前有效状态、已验收能力、剩余缺口和下一步。接口级路径、字段和页面调用详情见 [Swagger 匹配矩阵](./api-swagger-match-matrix.md)，开发规则见 [AGENTS.md](../AGENTS.md)。历史开发过程由 Git 记录，不在本文重复追加日期流水账。

> 后台协作入口固定为 [https://testhou.merchantsale.store/](https://testhou.merchantsale.store/)；需要审核、准备测试数据或后台回读时直接使用该部署站，不启动或访问本地后台管理服务。后台登录账号以后台项目文档中明确记录的开发、测试或演示账号为准，不在本文复制凭据。Knife4j 契约入口单独维护，不与后台管理站混用。

## 当前承接状态（2026-08-29）

本阶段稳定性补充：可接求购、收藏、秒杀报名、分类申请读取已统一加入账号切换隔离、请求取消与最新响应保护；求购/分类申请页码越界只回退一次，分类树读取与列表读取互不覆盖。现有提交、报名、取消交互保持不变。

当前前端交互语义已同步收口：公开商品/公告、钱包流水与资产桶、IM 会话、个人中心、布局品牌入口、分类侧栏、订单详情、买手订单和理财产品入口均支持键盘导航与焦点反馈；不改变既有路由、数据读取或资金/订单写入流程。

真实 API 的分页与列表适配层已统一校验必需数组结构，覆盖订单、商品、地址、分类、积分、理财、押金、充值/提现和求购等读取；缺少 `records/list` 或类型异常时进入既有错误态，合法空数组仍保持为空态，不改变正常分页与业务交互。

通知未读数对接口和 WebSocket 的异常数值改为保留最近一次有效角标，不再把 `NaN/Infinity/负数` 重置为 0；合法数字和无计数推送的递增行为保持不变。

本轮继续补齐登录/注册、VIP 积分、个人中心公告与订单、结算协议、IM 独立会话、订单商品和评价“查看订单”等已有 `role="link"` 入口的空格键触发；与 Enter 行为一致，不改变路由、接口调用或业务写入。

Chrome 本地只读回归已验证登录页“立即注册”与注册页“返回登录”可用空格键完成路由切换，认证布局品牌入口可用空格键返回首页；未触发登录、订单、资金或消息写入。

登录/注册、充值申报和采购凭证确认入口补齐函数级重复提交保护；在快速点击或重复键盘事件下只允许当前操作进入请求，正常校验、接口参数和成功回读均保持不变。

充值、提现、保证金、订单改价、秒杀报名、求购和买手商品表单补齐数字输入的有限性与边界校验；清空或输入 `NaN/Infinity` 时只提示修正，不进入确认弹窗或请求，确认金额展示不再依赖不安全的 `toFixed`，正常金额和接口参数保持不变。

押金环图、评分组件、理财曲线和年化展示对异常数值增加安全降级；非法响应不再渲染 `NaN`、无效 SVG 坐标或错误百分比，合法数据的显示精度和业务操作保持不变。

商品详情图库缩略图补齐按钮语义、当前图片标识和点击/Enter/空格选择；原有悬停切图保持不变，未改变商品详情读取或购买流程。

Chrome 本地只读复核商品详情图库：当前真实商品返回 1 张图片，缩略图节点的按钮角色、焦点和当前标识正常，空格操作不改变路由或业务状态；未将单图数据冒充多图切换验收。

2026-08-29 18:57 本地 Chrome 只读 smoke 覆盖 51 个公开、登录、买手、资金、售后和 IM 路由壳：全部产生非空首屏，未出现 Vite 错误覆盖层或 `Internal server error`；未登录受保护路由统一跳转登录并保留 `redirect`。该证据只证明路由与错误边界，不替代已登录业务数据回归，未执行任何写入。

写入成功与后续只读回读已解耦：转出申请创建成功后，详情或余额刷新失败不会误报为提交失败；KYC 提交成功后，账号状态刷新失败只提示稍后重试并保留成功结果。钱包总览或最近流水的当前请求失败时清理对应旧快照，避免继续展示过期余额或流水；任一并发分支本次成功的数据仍正常显示。

首页真实区块读取失败时清理对应旧快照并保留明确重试入口，避免将上一次成功内容误认为当前实时结果；成功区块与首页正常展示不变。

全局会话初始化已加入单飞、身份版本和刷新序号闸门；登录、退出、演示切换和资料刷新不会被旧的 `/auth/me` 回包覆盖，同一账号的并发刷新也只允许最新结果写回。VIP 页面切换账号时立即清理旧等级状态并使旧请求失效，权益展示口径不变。

KYC 详情及私有文件签名地址读取同步加入账号切换隔离与请求取消，切换账号时清理旧账号证件预览；认证提交流程保持不变。

首页推荐、榜单、秒杀和 Banner，以及支付成功页订单/推荐读取增加组件卸载与路由参数切换保护；首页展示和结算写入流程保持不变。

通知未读数刷新增加请求版本与 token 校验，账号切换或断开连接后旧请求不会覆盖新账号角标。

购物车商品详情刷新、结算页钱包读取增加组件卸载与账号切换隔离；结算下单、支付和幂等恢复逻辑保持不变。

购物车 Store 商品详情刷新在账号切换或清空时主动失效；旧账号的商品响应不会写入当前购物车缓存，购物车与结算交互保持不变。

公共分类树读取（顶部导航、侧栏、分类页、买手商品表单、求购表单）统一支持请求取消与最新响应保护；导航读取失败不再产生未处理 Promise，分类选择与表单提交交互保持不变。

商品详情的评价列表、评价汇总和买手评分，以及通知关联评价详情统一透传取消信号；通知 query 切换或离开页面时清理旧关联结果，评价列表与商品详情交互保持不变。

个人中心的 VIP、钱包和订单统计读取统一透传取消信号，并在账号切换/卸载时失效旧请求；公告仍按既有演示数据展示，资料保存流程不变。

钱包 Store 总览和通知未读数刷新同步支持真实请求取消；退出、断线或账号切换时不会继续保留旧账号的后台读取，角标与资产展示口径保持不变。

钱包流水与买手钱包页同步监听用户 ID 和身份视角；账号切换时清空旧列表、重置读取并保留现有筛选与资金操作。

买手订单与评价页同步监听用户 ID；账号切换时清理旧列表、页码和详情弹窗，再读取新账号数据，订单和评价写操作保持不变。

买手商品页与订单列表同步监听用户 ID/身份视角；账号切换时清理旧商品、订单、统计和可评价集合，再按当前账号重新读取，商品和订单操作顺序保持不变。

支付成功、通知和仅退款列表/详情读取同步校验当前用户 ID；账号切换时清理旧订单、通知和退款数据，原有跳转、已读、撤销交互保持不变。

消息中心在账号 ID 变化时失效会话/消息分页与增量请求，清理旧会话后重新读取当前账号；IM 发送、撤回和已读交互保持不变。

买手售后列表读取同步校验当前用户 ID；切换买手账号时清理旧售后记录并重新读取，筛选与撤销交互保持不变。

理财持仓列表的状态数量徽标同步支持请求取消与账号切换隔离；旧账号的持仓数量不会覆盖当前账号，赎回及列表交互保持不变。

VIP 页公开配置、积分规则和当前等级读取统一透传取消信号；账号切换或离开页面时不再保留无效读取，配置或规则分项失败时保留成功数据并明确提示重试，权益展示口径不变。

KYC 页当前用户资料刷新同步透传取消信号；账号切换或离开页面时不会让旧会话的 `/auth/me` 结果覆盖全局用户状态，认证提交顺序保持不变。

理财产品/锁仓详情、订单详情、求购详情和订单群同步校验路由参数与当前用户 ID；切换账号或详情路由时清理旧数据并失效旧读取/消息响应，现有订阅、解锁、订单和消息写入交互保持不变。

订单群的历史消息分页与实时增量读取新增独立请求闸门、取消信号和失败保留当前消息处理；切换订单/账号或组件卸载时立即失效旧请求，消息内容、滚动和发送交互保持不变。

评价图片上传回调新增组件卸载与操作版本保护；页面切换或上传上下文失效后不再把旧图片地址写回当前评价表单，也不会错误结束新一轮上传状态，评价提交交互保持不变。

IM 媒体上传与录音回调按会话版本和具体 `MediaRecorder` 实例隔离；切换会话或组件卸载后旧图片/语音请求不会清掉当前上传状态或覆盖新录音，发送交互保持不变。CMS 协议弹窗按显隐与协议类型版本隔离，旧协议响应不会覆盖新选择。

KYC 证件上传按组件生命周期与重新上传操作版本隔离；离开页面或点击重新上传时，旧响应不会覆盖当前证件预览或恢复过期上传状态。公告轮播清理组件定时器，帮助文章详情的演示请求按操作版本隔离，页面快速切换不会遗留定时器或旧错误提示。

AI 导购演示请求在组件卸载后不再写入消息或错误态；公告详情在筛选/分页刷新及快速切换时按操作版本隔离，分类导航清理悬停延时器，结算余额不足跳转在离开页面或账号切换时取消，均不改变现有业务路径。

VIP、求购详情和我的求购在等待会话初始化期间支持组件卸载失效；页面离开后不会重新启动读取或写回已卸载组件，现有列表/详情交互保持不变。

理财我的锁仓在列表读取完成后再启动数量读取前检查组件是否已卸载；离开页面不会重新启动过期的数量请求，锁仓/赎回交互保持不变。

通知中心的筛选/分页导航回调与通知标记已读跳转按组件生命周期和操作版本隔离；页面离开或账号切换后不再重新读取或执行过期跳转，通知展示和正常跳转行为保持不变。

积分页在 VIP 状态、流水和规则串行读取之间检查组件生命周期与最新请求；页面离开或账号切换后不会启动后续过期读取，积分/申诉交互保持不变。

消息中心刷新会话在会话列表与当前会话同步的串行步骤之间检查组件生命周期；页面离开后不会启动过期同步，实时消息与刷新交互保持不变。

消息中心在会话列表完成首个会话选择后再次校验列表请求有效性，并在选会话、已读上报和增量同步入口拒绝已卸载组件，避免过期响应继续推进实时读取。

赎回锁仓、撤销求购后的串行列表刷新增加操作版本与账号检查；切换账号或离开页面不会再启动旧会话的下一步读取，原有成功回读保持不变。

钱包流水切换账号时主动失效旧列表请求并关闭旧详情；充值/提现切换账号时同步失效列表、链配置、地址和详情请求并清理旧详情，资料保存失败由页面安全收口请求异常，协议演示请求展示明确错误态，公告分页越界只回退一次，均不改变正常业务交互。

买手钱包在退出或切换账号时主动失效旧钱包/流水请求并关闭旧流水详情，避免匿名或新账号页面被旧会话回包覆盖。

评价列表和资金流水在无用户会话时也先失效请求闸门，直接触发重试或退出不会再让旧账号读取回包写入当前页面。

账户退出或切换期间，各登录态列表、详情和专题页在无会话闸门处同步清空旧业务数据并复位 loading，避免请求已失效后页面永久转圈或残留上一账号内容；正常接口和写入流程不变。

| 口径 | 当前状态 |
|---|---|
| 分支 | `main`；准确提交位置以 Git `HEAD` 为准 |
| 结论 | **部分完成**；新契约代码适配、可用 QA 写入回归及 WebSocket 双账号页面级实时回归均已完成。本轮已完成隔离买手订单发货、物流详情、通知和订单群回读，并复跑实时契约、类型、既有测试与生产构建；本地与部署 C-PC 静态路由、动态商品/订单/账户/买手读取回归均已完成。剩余主要是该隔离订单顾客侧签收/评价会话、充值地址读取与真实链上出款条件 |
| 实时 Swagger | admin `178/179`、user `46/46`、order `65/67`、notify `22/22`（路径/操作）；`pnpm check:swagger` 通过 |
| 实施进度 | 约 `95%` |
| 真实验收 | `29/35` 项能力，约 `83%` |
| 主链路 | 地址、下单、订单组支付、签收、仅退款已完成既有双账号真实回归；本轮新增隔离买手订单发货、物流、通知和订单群回读，待该订单所属顾客会话完成签收/评价 |
| 本批完成 | 新契约适配：商品卖家/分类与审核字段、求购审核/指派字段、全量物流承运商与轨迹/异常管理、订单物流空态、通知详情跳转、READY 心跳协商、积分默认规则字段；已完成 IM 媒体上传/撤回、已下架商品删除、隔离订单支付及 Chrome/Safari 双账号实时回归。本轮补充了商品提交、订单支付/取消/签收、买手发货/物流、售后、收藏、求购、地址、充值申报、押金、KYC、评价、通知与 IM 删除等真实写操作的确认态和请求态互斥；修复公开 VIP 页未初始化真实登录态、误展示“登录查看等级”的问题。2026-08-29 本地完成 51 条路由定义的壳 smoke；本地与部署各完成 41 条可达公开/登录/受保护业务静态入口逐页回归；本地无会话受保护页正确跳转登录，部署已登录买手会话的动态商品筛选与详情、订单详情、钱包/评价空态、通知、IM、买手商品筛选均正常，控制台无新增 warning/error。隔离买手已对订单 `2093370220756951040` 完成发货并回读物流、通知和订单群；本轮继续补齐商品、订单、买手订单、售后、求购、理财详情和订单群读取的最新响应保护与卸载取消，商品详情失败与不存在状态分离，购物车 Long ID 比较统一，通知未读数增加下限保护；并收口钱包原始 `bizType/bizGroup` 透传与真实类型展示、订单/钱包多类型深分页分批读取、登录失败 token 回滚、钱包首页部分并发失败隔离、商品/收藏/求购页码越界一次回退及充值链选择空值告警；本次承接追加买手工作台与押金页的账号切换/卸载取消、部分并发失败隔离，避免旧账号响应覆盖新账号且不把流水请求失败误显示为空态；本轮继续补齐理财产品/概览、充值链与地址、充值/提现详情及分页读取的账号切换隔离、请求取消和独立详情竞态保护，并为地址页账号切换及商品图库失败图片增加统一失效隔离；本轮追加积分流水、申诉、规则和 VIP 读取的账号切换隔离、请求取消与越界页一次回退；应用层在账号 ID 变化时主动重置并重连 IM，避免登录态仍为 true 时沿用旧 token；以上读取保护不改变任何资金写入流程；复跑 `pnpm check:swagger`、`pnpm typecheck`、`pnpm test`（9 文件/24 项）及 `pnpm build` 均通过 |
| 当前阻塞 | 已核对两条 C-PC 文档账号在后台是不同用户；本轮选取的在售商品属于当前买手身份，顾客侧按预期触发自购拦截，因此未建立新的跨账号支付链路。已新建独立顾客并完成 C-PC 登录、后台用户台账确认，但后台未为该真实用户生成钱包记录，前台仅显示零资产且后台无可调账对象，仍不能支付；充值页专属地址读取失败，无法建立真实 `PENDING` 申报；提现真实链上到账缺 TRON 转账及对应 `txid`；CMS/AI 缺 C 端契约；响应式验收按当前决定暂缓 |
| 下一步 | 后端为独立顾客创建钱包并提供测试余额，或提供隔离订单所属顾客的测试会话后完成跨账号下单、发货、签收、评价与买手侧回读；后端恢复充值地址读取后完成申报取消回归；取得真实 TRON `txid` 后完成提现到账回读；继续处理可独立验证的加载竞态、错误态与空态收口；CMS/AI 仅在提供 C 端契约后接入 |

当前必须保留的继续数据：

- 待打款提现单：`2090377523934879745`，状态已由后台审核为“待打款”。
- 该单只有在真实 TRON 转账完成并取得对应 `txid` 后，才能在后台确认已打款；禁止伪造哈希或未转账即标记到账。
- 秒杀 QA 场次已完成报名/取消回归并在后台停用，不影响后续正常场次。

## 梯队进度

本表为此前业务能力统计基线，不包含下方多批异常交互整改的完整验收。表内约93%与顶部约95%是历史估算，尚无统一逐项分母，不能作为当前精确实施率；29/35同样不等于本轮整改目标完成率。当前新增证据和未验收分支以对应整改表为准，不因局部回归通过提升整体百分比。

梯队百分比按“契约确认、API/核心逻辑、页面接入、真实数据验证”综合评估；接口存在但未页面调用、只有空态或缺写操作回归时，不计为完整验收。

| 梯队 | 实施完成度 | 真实验收 | 已完成 | 剩余边界 |
|---|---:|---:|---|---|
| P0 联调地基 | `100%` | `100%` | 请求层、登录态、Long ID、地址、结算、批量下单和订单组支付 | 无前端代码待办 |
| P1 基础资料 | `95%` | `85%` | 当前用户、分类树、积分账户/规则/流水、VIP 当前状态/配置、钱包总览、KYC fileId 提交与详情访问刷新 | KYC 测试资料和公开配置的非空业务回归 |
| P2 商品/求购/买手 | `99%` | `95%` | 商品上传/创建/删除/审核/公开展示、上下架、收藏、分类申请、求购审核状态、秒杀报名/取消 | 历史比例不是本轮验收数字；管理端已有分配/推送/批量/日志，缺口是买手收到的求购查询 |
| P3 交易闭环 | `100%` | `92%` | 购物车、地址、下单、支付、改价、发货、全量承运商、物流轨迹/异常管理、签收、订单列表/详情、仅退款全分支 | 非空物流轨迹和凭证写入回归 |
| P4 资金与会员 | `90%` | `75%` | 钱包、流水、充值测试到账、提现申请/驳回/审核、买手押金缴纳/退还 | 提现链上到账 |
| P5 专题能力 | `90%` | `78%` | KYC、理财、评价、通知 REST/详情跳转、IM REST 新媒体上传、WebSocket 双账号实时、`READY/PING/PONG` 协议及开发代理 | CMS、AI |
| **整体** | **约 `93%`** | **`29/35`，约 `83%`** | 前端主体和主交易链路已收口 | 主要为后端/环境条件 |

## 已完成与验收结论

### P0/P1 基础能力

- 已建立 admin、user、order、notify 四域真实请求层，统一处理 `X-Access-Token`、响应解包、业务错误和登录失效；契约检查已覆盖上传 scene、KYC fileId、IM mediaFileId、物流、求购状态和通知字段。
- 邮箱登录、注册、当前用户初始化、退出和买手/顾客身份切换已接真实接口。
- 真实会话、钱包、订单、商品、求购、押金、积分和 VIP 类型已与 Mock `number` ID 模型隔离；业务 Long ID 保留原值。
- 分类树、地址新增/编辑/设默认/删除、积分账户/流水/申诉、VIP 当前状态和钱包总览已页面接入并完成真实读取或写入回归。
- KYC 已切换独立 `/kyc/files/upload`，提交 fileId，详情按 fileId 调 `/kyc/files/access` 获取短期签名地址；不缓存过期地址，也不以真实个人证件作为普通回归前置。
- 2026-08-26 Chrome 已回读 C 端公开积分规则和 VIP 全量配置：VIP0/VIP1/VIP2 门槛与顾客/买手权益均非空展示，当前 `john / 100` 积分进度与积分页一致。

### P2 商品、求购与买手

- 买手商品已完成 PRODUCT scene 图片上传、创建、删除、后台首次审核、公开列表/详情、上下架及跨账号购买回归。
- 商品收藏已完成新增、列表回显和取消；浏览打点失败不会影响详情主流程。
- 分类申请已完成提交、后台审核、新分类树节点回读和商品创建选择联动。
- 求购已完成创建、列表/详情、取消、买手大厅、抢单及关联订单回读；新增 `PENDING_REVIEW` / `REJECTED` 状态、审核意见/时间和可选后台指派人展示，创建后不再假定直接进入大厅。管理端已有分配/推送/批量/日志，不能当作买手端“推给我的求购”查询；保留 C-PC 现有业务边界。
- 秒杀已完成后台启用 QA 场次、买手以 `90 U / 库存 1` 报名、列表回读、取消报名和后台停用；毫秒时间戳已统一格式化为本地时间。

### P3 主交易链路

- 已使用独立买卖双方完成真实商品加购、地址选择、批量下单、订单组支付、买手发货和顾客确认收货。
- 自购商品在前端预拦截，后端仍作为最终校验；购物车按账号隔离，失败请求不生成本地假订单。
- 买家和买手订单分页、状态映射、改价、发货新 carrier 契约、真实物流详情/轨迹、订单详情及支付成功页已使用真实数据；物流页面不再从订单时间线推造轨迹。
- 买手发货表单已适配完整 carrier 枚举及 `carrier/carrierName/trackingNo/eta/purchaseNo/purchaseVouchers/shipVouchers/remark`；买手订单已接轨迹登记和异常标记，待有可发货订单时验证真实写入。
- 仅退款已完成申请、买家撤销、后台驳回、后台同意、订单状态、钱包资金和退款流水回读。
- 支付、发货后可能存在短暂读模型延迟；回归应等待稳定后刷新，不能将首次空结果直接判为前端失败。

### P4 资金与会员

- 钱包总览、资产桶、今日收支、最近交易、资金流水、筛选、详情和 CSV 导出已接真实接口。
- 充值已完成测试环境模拟到账、充值详情和钱包流水回读；必须明确标记为测试模拟回调，不表述为真实链上到账。
- 提现最低金额按后端真实规则为 `20 U`。
- 提现单 `2090358267394482178` 已完成“申请 -> 后台驳回 -> 可用余额返还 -> 流水回读”。
- 提现单 `2090377523934879745` 已完成“申请 -> 后台审核通过 -> PC 待打款回读”，最终链上步骤尚未完成。
- 买手押金已完成真实缴纳、余额/流水回读、退还和余额恢复。

### P5 专题能力

- 理财已完成产品读取、申购、持仓、提前赎回及资金变化回读；2026-08-22 使用隔离 QA 产品 `2089330973230063617` 完成“后台上架 → C-PC 可见 → 后台下架 → C-PC 不可见”回读，并恢复为 `OFF_SALE`。产品页仅展示 `ON_SALE` 项，无可申购产品时显示空态。
- 评价已完成待评价校验、提交、删除、买手回复、买手申诉、后台裁定和前台裁定结果回读；买手订单和已评价订单不再错误展示“写评价”。
- 通知已接分页、未读数、已读、全部已读、删除和清空；充值、提现、评价的通知可按 `id` 直达详情，业务跳转仍依赖后端返回 `bizType/bizId`。
- 通知新编码仅在 `bizType + bizId + templateCode` 三项满足新枚举时跳转；历史空值、旧编码和未知值保持不跳转。
- IM REST 已完成会话/消息分页、按订单取群、文本/图片发送、已读、撤回、刷新回读和 Long 安全解析。
- IM 媒体上传已切 `/notify/im/files/upload`，发送消息传 `mediaFileId`，语音时长在上传时提交；WebSocket 使用 `im` 子协议携带 token，收到 `READY.heartbeatIntervalMs` 后按服务端上限协商心跳。2026-08-26 已以隔离 QA 账号验证直连与 Vite 代理均完成 `101 → READY(25000) → PING/PONG`。
- 同日以 Chrome john 与 Safari mamba 的独立真实会话完成浏览器端业务回归：同一订单群消息无刷新到达并在发送端回读已读，撤回后对端实时替换为“消息已撤回”；Chrome 离开消息页期间发送的隔离消息，在重新进入后通过补偿回读显示。测试消息均已撤回，两个页面控制台无新增 warning/error。

## 关键验证证据

| 能力 | 当前有效证据 | 结论 |
|---|---|---|
| 主交易 | 双账号真实商品下单、组支付、发货、签收 | 通过 |
| 仅退款 | 申请、撤销、驳回、同意及资金/库存回读 | 通过 |
| 充值 | 单 `2090357697803804673` 测试模拟到账并生成测试哈希 | 测试环境通过，非真实链上 |
| 提现驳回 | 单 `2090358267394482178` 驳回后余额与流水恢复 | 通过 |
| 提现通过 | 单 `2090377523934879745` 后台审核通过，PC 显示待打款 | 部分通过，缺真实 `txid` |
| 评价 | 提交/删除/回复/申诉/后台裁定/前台状态 | 通过 |
| 秒杀 | QA 场次报名、回读、取消，场次已停用 | 通过 |
| IM REST | 文本/图片发送、已读、撤回、双方刷新回读 | 通过 |
| IM 实时协议 | 直连与 Vite 代理均返回 `101`，协商 `im`，收到 `READY(25000)` 且 `PING/PONG` 正常；Chrome/Safari 双账号页面已完成实时到达、已读、撤回和断线补偿 | 通过 |

最近适用检查：

- `pnpm typecheck`：通过。
- `pnpm test`：9 个文件、24 项通过。
- `pnpm check:swagger`：通过。
- `git diff --check`：通过。
- 2026-08-29 页面变更已用 Chrome 回归 41 个可达静态入口及新增动态详情：公开商品/分类、账户资料、购物车/空结算、订单空态、地址、钱包/流水/充值/转出、积分/VIP、KYC、理财/锁仓、公告/帮助、售后、求购、评价、通知、IM、买手工作台/商品/分类/秒杀/押金/订单/售后/接单/钱包及认证页；首页推荐/榜单、收藏、可接求购、买手申请、分类申请、支付成功和 KYC 错误态均复核，无新增控制台 error。响应式、真实发货、充值取消、链上到账和 KYC 提交仍按外部条件另行验收。另有 51 条路由定义已完成壳 smoke，二者不重复计数。

## 未完成与后端补充清单

用户当前要求：缺失能力及研发缘由只在本表维护，不在业务页面展示技术说明或假操作。以下区分缺接口、已有接口未开放入口、临时读取失败与真实空数据；隐藏入口不代表后端问题解决。2026-09-06 显示层收口不修改请求参数、业务 ID、权限、金额确认或提交保护。

| 问题 ID / 优先级 | 页面 / 缺失能力 | 证据及影响 | 前端处置 | 责任方 / 恢复条件与验收标准 | 状态 |
|---|---|---|---|---|---|
| PC-01 / P0 | 提现：真实 TRON 出款和 txid | 既有记录读取不证明真实链上到账，不能伪造交易哈希 | 保留真实状态、金额未知及资金核实保护，不显示研发过程说明 | 链服务/后台：提供真实测试转账及 txid，核对到账、费用、时间与流水 | 外部条件未完成 |
| PC-02 / P1 | 发货：采购/发货凭证；独立采购上传 | 现有发货支持凭证，独立上传按钮没有动作；非空上传闭环仍未验 | 移除独立上传假按钮，保留填写发货和可随发货上传的业务提示，详情真实凭证不删 | 后台/QA：隔离订单与可上传条件，上传→发货→双方详情回读；独立上传如需恢复须有明确契约及页面调用 | 发货已接入待验；独立入口未开放 |
| PC-03 / P1 | 钱包/积分/求购筛选与 total | 9月5日契约和页面适配已补齐；9月6日钱包40条→单选小金库存入1条，URL为types=FINANCE_LOCK，刷新/详情关闭保持，重置恢复40条；旧误选未复现，不能认定页面缺陷 | 正常计数仅在加载成功时显示；账户/类型用业务名称，参数和ID原值不改；无钱包代码修改 | 当前筛选页CSV实际下载321字节、1行，11列、Long ID、类型、金额、余额及时间与页面一致；文件已清理。非空深页、复合边界及失败/在途导出待验 | 筛选提交与单行实际导出已验，深规模边界未验 |
| PC-04 / P2 | 可接求购：定向查询；求购进度 | `/demands/my/progress` 已验证提交/取消节点；管理端分配/推送/批量/日志已有，不等于买手收到的求购列表 | 保留真实大厅、权限和进度，不越权使用管理 API | 后台补买手定向查询及名称字段，按契约验证归属和非零样本 | 进度已接入，买手定向查询仍缺 |
| PC-05 / P5 | 公告/帮助/个人中心公告/协议 | 最新 user/order/notify 未发现 CMS 公告帮助路径；协议仍本地示例，不是正式版本 | 删除个人中心空公告卡；旧公告/帮助路由仅简短不可用态及安全返回；协议保留非正式演示声明、正文与同意流程不改 | 后台/法务：公告帮助查询及正式版本化协议；恢复真实内容后核对列表、详情、版本与同意流程 | 本期延期，正式内容未完成 |
| PC-06 / P5 | AI 导购 | 最新 C 端契约无 AI 查询/会话能力，模拟业务已停用 | 旧路由保留简短“暂不可用”与返回，无模拟对话/推荐或研发说明 | 后台：真实会话/消息/状态契约；前端接入后验证请求、失败、历史及身份隔离 | 本期延期 |
| PC-07 / P5 | 通知详情跳转 | 已读通知跳同订单有证据；9月8日后端称存量字段完整，不能再笼统认定当前缺字段，需具体通知ID复现 | 保留缺字段/未知编码安全处理和既有通知功能，不猜ID | 逐业务核对同实体；admin买手申请detail已存在，属于管理端接入；充值通知只属于当前C用户 | 部分验证，历史问题待指定样本复验 |
| PC-08 / P2 | 商品详情：同店推荐、售前联系、卖家等级 | 实时 `/storefront/products/page` 无 sellerId；notify 仅会话分页/按订单查询/删除，无售前建会话；此前卖家 VIP1 为固定展示非真实等级 | 移除同店空模块/tab、无动作联系按钮及固定 VIP1；保留真实评分、商品/评价、订单群联系路径 | 后台/前端：补卖家查询、售前会话和真实等级来源；接入后核对卖家归属、可联系对象、分页和实际等级 | 缺能力，入口隐藏 |
| PC-09 / P2 | 买手商品：编辑 | 实时产品路径只有创建/上下架/删除/查询/收藏，未提供商品修改接口，旧编辑按钮 disabled | 删除编辑假按钮，正常上下架/删除保护不变 | 后台：明确编辑字段、审核语义与权限；前端提交后回读原商品并验审核状态 | 缺接口，入口隐藏 |
| PC-10 / P0 | 结算：钱包以外支付 | 当前 pay/group/pay 为钱包流程，没有已接入的 OKX 支付契约 | 删除 Swagger/未来 OKX 技术说明，仅保留可用钱包支付；余额/自购/协议/金额确认与未知结果保护不变 | 支付后台：外部支付创建、状态查询、回调及幂等契约；前端接入并验证取消/失败/结果恢复 | 外部支付未开放 |
| PC-11a / P1 | KYC 关联文件 | 9月8日后端称 detail 缺图返回null并保留状态/FileId，历史原文件未恢复，只有编译证据 | 三图URL类型允许null；保留逐文件私有access隔离失败和状态，各缺图显示无可用预览，不开放额外重提 | 后端/QA核对存储/签名访问及指定测试材料；不换URL主机绕过权限 | 前端兼容完成，真实缺图/签名/上传未复验 |
| PC-11b / P1 | 充值专属地址与申报 | 9月8日回复指明系统资金钱包配置chain.callback.base_url仍空，链服务开关也是前置条件；chains.depositAddress是平台地址而非专属地址 | 源码复核页面只使用recharge/address所得address，不回退平台地址；该GET会分配地址，本轮未调用 | 后台/运维提供回调和链服务条件后，再授权隔离PENDING申报/取消 | 外部环境阻塞，未声称恢复 |
| PC-11c / P1 | 理财产品与申购 | 9月5日为空列表且后台测试账号缺产品权限；9月6日纠正误访 `/finance/products`（详情id=products），正式 `/finance` 自然页面为暂无可申购产品、锁仓/待结算收益0.00，Network三个子请求均HTTP200。随后独立API读取确认同一文档QA身份：三个接口均code=1，products/list为数组且0项；收益4个金额字段为合法零字符串、holdingCount为数值0；钱包distribution内FINANCE_LOCKED.amount为合法零字符串。这是另一次API读取，不是同一页面响应体捕获。3A原已赎回锁仓详情实际累计收益0.00，曲线却显示“已累U0.31”；源码曲线按年化及当前经过天数估算，尚未随赎回终止 | 产品/收益/钱包失败分别提示；收益概览失败清旧值，缺失pendingInterest不补0；保留正常金额与重试，不新增错误地址别名。曲线问题本批仅记录，后续核对赎回时间及理论/实际收益展示语义，不改业务计息 | 非空申购仍需隔离在售产品及后台权限；独立受控失败/重试及缺字段页面分支待验。赎回曲线语义属前端待核实修正，不据此推断后端继续计息或资金损失 | 前批源码修正通过类型及184测试；正确入口自然空态与独立API零值已验。3A本地原REDEEMED锁仓详情100及赎回按钮禁用已验，新增异常分支未验 |
| PC-12 / P5 | IM 风控规则查看 | 风险提示组件原有 disabled 规则链接无目标；风险消息及交易警示仍有业务意义 | 仅删除无动作规则链接，不改风险警示、媒体、已读或消息数据 | 产品/后台：提供有效规则内容/版本与入口，验证可达及适用说明 | 规则入口未开放 |
| PC-13 / P2 | 买手工作台层级及通用展示 | 真实统计卡此前置于 hero 前；多页错误态含研发说明，理财tooltip/秒杀placeholder亦有技术词 | 恢复 hero→KPI→经营统计层级并保留真实指标；研发附言移除、错误/重试保留；钱包账户与积分复用业务标签 | 9月6日 Chrome 已验证工作台层级、钱包行与抽屉、类型选项、积分标签、求购计数、个人中心公告卡移除、不可用页面返回及协议声明；保留金额/ID/权限/三级分类保护 | 代码完成，定向页面已验；非全51路由验收 |

本次仅显示层修改；已通过类型检查、既有 9 文件 184 项测试及 git diff --check。定向检索覆盖全部 Vue 页面、公共组件及布局，技术词残留为代码注释；普通认证/支付/未知结果警告继续保留。最新契约只读核对用于区分问题性质，未更改 API。Figma 对照链接无法通过只读工具打开，不作为视觉验收证据。9月6日复用 Chrome 本地 localhost:5173 已登录会话，定向验证范围见 PC-13，控制台 warning/error 为空；仅操作自建标签页并已关闭，未切换账号或执行业务写入。本轮浏览器使用已结束。

本轮未验边界：PC-03筛选/单行CSV已补验；插件勾选失败后通过选项文字正常选择，插件下载等待无结果后经原生Chrome按钮实际下载，不将工具限制认定为业务故障。理财响应详情读取期间用户切到H5标签，停止原生点击；现接口无后台响应体/局部拦截能力，未继续争抢焦点，已关闭唯一自建标签并释放浏览器。商品详情（进入会记录浏览）、IM/通知写入、凭证/KYC上传、资金操作及深分页本批未执行。协议正文/同意不变。当前理财修正及对应文档按部分完成阶段交付，实际提交与远端位置以Git为准，未发布、未改顶部承接状态。

## 交互保护与剩余定向验收

本节合并此前多轮审查的重复记录。代码实施与页面验收分开；9月5日新增契约以接入结果表为准，不再把幂等查询、全量筛选、评价资格和显式 IM 已读记为缺接口。未列为已验的异常分支继续保留，不能由普通成功场景替代。

9月8日定向整改：商品/求购共用完整启用三级选择规则，提交前刷新启用树并复核，读取失败/失效不创建且保留表单；HTTP错误保留业务code，首次HTTP400的-311也不清原结算意图，不自动换键。请求错误追加可选traceId（兼容重复逗号串），不记录请求/凭据或加常驻提示。IM分页/增量/迟到消息及两页面本地/实时撤回统一清正文、媒体、时长、FileId和params；撤回图片退出已打开预览，原撤回占位保留。后端声称读接口清理不等于旧媒体直链撤权，真实新撤回仍待验。

后端回复与Swagger不一致项保留：create/create-batch同键异参-311仅回复明确，Swagger仍描述返回旧单；KYC/IM空值声明待同步；Trace响应头不证明全业务验收。管理端finance/reports/overview与买手申请detail已存在，不等于C-PC新增能力；支付密码当前无后端业务模型，不列强制补接口。理财数组/空态原修复保留，未造在售产品或修改计息。

本轮检查：9月8日四域Swagger通过（admin202/203/338、user50/50/92、order73/75/113、notify22/22/35），类型检查、既有9文件205测试、生产构建和diff检查通过，保留原Arco583.19kB提醒。只扩展既有测试；首次两个失败源于响应替身未提供新增读取的Headers，补齐Headers后原超时/主动取消断言保持通过。未提交/推送/部署或业务写入。Chrome恢复并由用户手动登录确认本轮测试会话后，复用127.0.0.1:5173桌面标签补验：商品与求购分类最初空态；用户随后补齐分类后，两页已实际逐级展开QA测试→QA-1→QA-3，一级/二级点击只展开而不填值，三级选中完整路径回显正确，当前完整启用三级链阻塞已解除。KYC正常显示已通过与原资料，三图分别暂无可用预览，刷新保持且无提交入口/整页资料读取失败；未捕获响应体，不能据此断言URL为null或具体私有资源失败原因。有效图/部分缺图/选填无材料、分类清除重选、失效提交、真实创建仍未验；商品清除图标工具点击仅关闭下拉且原值仍在，未确认清除动作成功，不据此判代码缺陷；求购未重复尝试。HTTP400冲突/刷新/跨账号、签名与上传、IM媒体撤回保留独立验收。导航/刷新初始曾短暂出现页面不存在，随后自然恢复目标页，未当作稳定失败或扩展修复。保留用户登录标签在KYC并释放Chrome，唯一开发服务保留；不以构建或空态代替业务闭环。

| 能力 | 当前保护及最近有效证据 | 剩余定向验收 |
|---|---|---|
| 结算对象与结果 | 原账号、内部订单ID、数量、地址、状态、金额二次核对；新结算与已建订单分离；部分成功不冒充整组成功。第三批3A只读核对本人组2096465008502857728/订单2096465008695795714：1笔PAID、已付6、未付0E-8；既有测试补合法科学计数零，未修改金额计算 | 新版金额确认、改价、部分付款、迟到/丢失响应、损坏缓存及付款后本地清理失败，均需真实隔离闭环；单笔已付组只读不覆盖这些分支 |
| 下单/资金互斥 | 3A同单退款/收货共锁及收货未知只读核对已交付；3B继续修复付款绕过：checkout两个付款入口在原pending锁内复用原订单定向锁，固定序、ifAvailable、不排队，覆盖详情预读/原校验/组或历史逐单付款/结果核对；详情与卡片单笔付款复用相同锁，无关订单/账号不受阻。原始付款POST不再导出，回调只能付原ID/原组，结束或会话变化即失效；任一锁失败释放已持锁。9项既有测试补充，独立付款绕过用例先失败后通过 | 3B代码及隔离测试完成，未做真实页面异常/跨标签/写入验收。在途互斥不等于跨刷新组付款未知结果恢复，也不代表跨浏览器/设备或后端业务原子性；后两者不以锁代码冒充完成。/orders/pay仅约定ApiResultLong，未说明回执是原订单ID，本批不擅加同ID断言；组金额变化/部分成功等真实闭环仍待验 |
| 退款/充值/提现/申购恢复 | 原键原参、当前身份回查；明确null才重试，-311保留。3A已修复：仅getItem===null判不存在，空串/null/false/0及损坏结构保留阻挡；资金已完成回执仍验证原键/账号/动作/快照；退款坏回执对象/布尔值/不安全数字不得冒充成功。既有测试先复现再修正；本人四类by-key只读探测均HTTP200/code1/明确null | 同键非空原单、冲突、响应丢失及页面恢复回读仍未完整验收；空探测键不证明历史原单恢复。新增保护仅代码与隔离测试，未修改真实浏览器存储或创建资金/退款单 |
| 押金及赎回 | 押金原方向/金额/键不变；3A补空字符串旧记录不被覆盖或换键提交；赎回仍仅同ID的REDEEMED解除未知标记。本人既有锁仓2089331913295224833只读为REDEEMED/本金100，产品2089330973230063617；在售产品列表0项 | 押金在途重开/跨标签恢复；非空赎回费用、迟到响应和真实结果恢复。既有REDEEMED读回及普通押金历史闭环均不替代这些分支；不自行上架产品或造资金状态 |
| 登录与身份 | 临时登录凭证验证成功后才切换；401/403/网络错误区分；旧会话不覆盖；受控 auth/me 503重试和401回跳已验。按用户要求移除登录页Mock账号列表、快捷登录逻辑/专用样式及示例账号提示，保留真实邮箱密码登录、注册和原回跳；Chrome本地/auth/login只读展示确认无Mock列表，类型及203项既有测试通过。用户授权推送前生产构建（含类型）和203测试复跑通过、diff通过，保留原Arco大块提醒；仅本页及两份文档提交，不部署 | 同时在途登录、旧写请求迟到、真实撤权/凭证到期与重连消息连续性；本次入口清理未重新提交真实登录，不扩全仓Mock删除 |
| 购物车与独立购买 | 账号隔离、同源锁、库存累计和付款快照清理；坏缓存不当空车；双标签加购/数量/勾选/删除同步已验并恢复空车；换商品数量重置已验 | 坏缓存页面恢复、付款后并发修改与真实存储失败 |
| 地址保存/选择 | 创建/编辑成功与回读解耦；共用选择器列表503后禁提交、重读恢复已验；25秒迟到保存不关闭新草稿已验；国际电话保存及QA清理完成 | 结算在途地址有效性复核、选择器空白/超长边界、真实多级地址组合 |
| 表单与确认弹窗 | 13处 Arco onBeforeOk、上传/提交互斥及打开版本；物流/分类/资料空值弹窗保留已验；商品/求购/退款离开提示和继续编辑已验；退款草稿同账号重登保留 | 发货/物流/秒杀/积分申诉/分类申请/评价回复弹窗慢写入重开；刷新关闭与成功提交放行；真实在途资料刷新 |
| 上传生命周期 | 批量保留成功结果、单独重试失败文件；外部重置失效旧回包；评价确认态只读；KYC按账号/证件/组件隔离 | 非空批量部分失败、上传中删除/切换、迟到文件、120秒超时及完整凭证提交 |
| KYC 配置/资料 | schema版本提交前复查；未认证QA表单及必填保留已验；认证通过但资料读取失败独立显示；不伪造文件预览 | 桌面文件选择受 Not allowed 限制，尚未上传；配置切换、驳回重提、签名刷新与非空预览见PC-11a |
| 钱包/余额未知 | 缺失账户、桶、金额保持未知，合法零保留；钱包503时成功流水保留，重试恢复；结算余额503禁提交，重读保留商品/地址/协议/contextId已验 | 成功响应缺字段的真实页面分支、理财余额失败/切换及资金写入故障 |
| 普通分页/多状态合并 | 真实size/pageSize/total，缺页/重复/变动不补假数据；来源串行、最多4源并行；URL非法枚举规范化、超大页码单次回退已验 | 超出现有规模的深分页、服务端缩页/末页、分类>24件、买手流水>30件、秒杀多场次同商品 |
| 筛选与导出 | 9月5日真实服务端条件已适配；历史39条两页无重复；9月6日40→小金库存入1条，刷新/抽屉返回保持、重置恢复40；单行CSV实际下载并核对原始Long/类型/金额/余额/时间一致，临时文件已清理 | 新契约后非空深分页、复合条件边界、在途/失败下载仍未验；不以单行导出覆盖这些分支 |
| 路由与订单视角 | 内部Long与展示号分离；非法query恢复，详情返回保条件；Safari买卖视角前进/后退及列表第二页恢复已验；404返回首页已验。9月6日真实求购成交订单2086783649773608962返回productId=null，详情已取消无效商品链接/键盘焦点，点击不跳转；卖出卡片仍进订单，仅纠正读屏标签。Chrome本人求购详情原生返回全部6条已验 | 权限实时失效、其他列表深页返回；输入法候选确认不触发搜索的真实浏览器场景 |
| 钱包在途切账号 | 真实第二页200响应延迟25秒时切账号，新页保留page=2并回读19/39；旧请求断开未覆盖，恢复原账号16/36已验 | 撤权及切换后页码越界分支 |
| 分类/商品 | Cascader保持单个Long；真实分类查询、分类“查看全部”、收藏幂等及取消清理已验；本人商品禁加购/购买、非本人可用已验。用户截图的详情Tabs遮挡已修复：父容器overflow:hidden下移除sticky多余64px偏移；本地Chrome同商品的详情/规格/评价首行及滚动返回均无遮挡，业务不变。类型、生产构建及diff通过，保留原Arco大块提醒；用户授权提交推送，不手动部署 | 分类新提交参数、多级/大规模数据、未知售后/积分类型真实样本、图库多图缩容/失效、评价深页及读取失败；本次为局部样式回归，未新增交易验收 |
| 首页/工作台附属加载 | 活动慢读/推荐503不影响其他栏目，重试恢复；工作台钱包慢读不挡订单；商品评价3分支延迟25秒不挡主体；结果页推荐延迟18秒不挡订单已验 | 非空活动/秒杀、采购中工作台发货入口、其他分支故障；理财申购成功后慢钱包跳转 |
| 评价资格/恢复 | 批量最多200按ID映射，详情权威资格；漏项不当无资格；按订单幂等及会话内快照恢复；已有已评价订单不开放表单已验。9月6日同一内部订单2093369377466966017只读API核对：卖家NOT_BUYER，顾客详情及批量均ALREADY_REVIEWED，关联评价2093492197668773889 | 身份/期限/删除资格动态变化、真实提交/丢响应/原参数恢复及回复申诉；本轮顾客资格仅API证据，非新浏览器写入验收；快照不落盘，不承诺刷新/退出后草稿恢复 |
| 通知 | 缺字段不猜目标；已读/删除/清空按筛选回读，业务跳转不等待已读。9月6日Chrome已读付款通知2093369380017098753进入内部订单2093369377466966017，展示号2093369377458577408；回读仍已读、角标6不变 | 各业务新完整字段、跨页删除/清空、慢读重复点击及在途账号切换 |
| IM 已读/撤回 | markRead:false + 可见到底显式已读；9月5日双账号文本非当前会话未读1、打开后总60→59、发送端已读1，撤回双端刷新不恢复已验。9月6日指定订单群2093369377576013825入页前未读0且已读水位等于最新2096203219533914113；Chrome8条历史中2条仅显示撤回提示，离页后未读/水位不变。旧服务端1条撤回记录仍带残余内容或媒体，前端现有终态清理未展示它 | 真正隐藏预取、新媒体撤回写入、独立群及故障重连；本轮仅现有历史展示，不代表新撤回闭环；入页可能触发显式read机制，未捕获全部网络请求，不称绝对无写；旧历史残余需后台清理 |
| IM 同步/锚点 | 独立REST游标，5批×200分轮补偿；不抢历史阅读；Chrome/Safari新文本位置保持/查看最新已验；摘要503不阻断增量、延迟已读不阻断输入已验 | 历史深分页/滚动在途/媒体尺寸变化、超过一批断线补偿、高频事件/快速身份切换 |
| IM 发送确认 | clientMsgId原值重试；WS已确认不被迟到HTTP失败回退；主会话/独立群发送200延迟45秒仍成功且无残留超时提示已验 | 真正失败重试、录音上传/第二账号播放、媒体并发；旧批4条QA文本3条撤回，1条超过2分钟窗口仍保留，不宣称全部清理 |
| IM 目标/空态 | ORDER_CARD保留Long，缺ID不跳；指定目标不存在不默认首会话；三方群→空售前/客服清URL及输入区已验 | 超过一页目标、在途切换和删除最后会话；不删除正式业务会话 |
| 注册/买手申请 | 注册邮箱/密码边界及BUYER角色守卫；批准申请不重复提交；未知申请结果只核实状态 | 注册成功迟到导航、审核变化/失败、申请响应丢失；不以空列表或旧状态自动解锁 |

所有受控失败/延迟证据均为隔离代理场景，不冒充真实服务故障或写入验收。既有QA地址和购物车临时记录已清理，消息超窗保留项如上；不在文档保存账号、密码、证件或Token。

## 2026-09-05 后端变更执行清单结果

范围为工作区根目录 `20260905_C-PC_后端变更执行清单.md`，基线 `5053b09`；已 fetch / pull --ff-only，无远端新增提交。以下为本轮实施状态，不替代顶部历史承接信息或完整业务验收。

| 清单任务 | 本端实施 | 本轮验证与剩余项 |
| --- | --- | --- |
| 1 付款 | 所有单笔/组付款调用传 confirmedAmount；金额敏感响应保留十进制原文，BigInt 合计；组付款保留逐单结果并校验完整性，部分失败不跳整组成功；回查/刷新恢复、同组剩余金额重付、-312 后重新确认；原账号下单记录保留，付款不重复创建订单；结果页回读组内状态与归属 | 类型、既有测试、构建验证；单笔/组成功、改价、部分失败、响应丢失、剩余重付及资金流水尚未真实验收 |
| 2 幂等 | 仅退款及提现/充值/理财均接入 idempotencyKey 和 by-key：原账号/参数/键持久化，成功回执保存；按当前登录人查询并校验原参数，明确 null 才原键原参重试；旧无键/损坏/失败不重发；-311 保留原键，-311/-312 不退出登录 | 三个 user by-key 真实空键查询均成功且 data 明确为 null；现有测试覆盖原键原参、回包不完整/冲突、账号切换、旧无键保护。未新建资金申请，真实同键返回原单/冲突及响应丢失恢复仍未验 |
| 3 承运商 | 动态字典排序/启用项、空白表单默认项、自定义名称规则、失败重读；新编码与历史名称快照保留；不回退固定 16 项 | 已登录真实接口返回 16 项，新契约与 adapter 测试通过；新增/停用/迟到字典和发货完整闭环未验 |
| 4 KYC | 已接登录态 schema；证件选项、国籍/背面/手持要求、驳回重提和说明共用配置；提交前刷新版本并重验快照，变化/失败保留草稿；保留私有文件 access，过期状态沿用后端 expireAt | 实际配置 version=4；两份本地测试账号 detail 均报“文件不存在或已删除”。Chrome 正确保留资料失败/重试及账号状态，不开放提交。动态组合测试通过，上传、提交、签名资源及后台切配置的真实验收未完成 |
| 5 IM | 分页显式 markRead:false，按纯读取处理异常；两种消息页移除加载即清未读，仅可见并到消息底部后显式已读，保持单调/账号隔离；撤回清内容/媒体，迟到原消息不能复活，null 不补旧值 | 双账号 Chrome 文本发送、非当前会话保留未读、可见阅读及撤回刷新已补验，见下文；真正隐藏标签预取、媒体撤回、独立群窗口及故障重连仍未完整验收 |
| 6 筛选 | 求购 statuses/预算/交付天数、钱包账户桶/关键词/日期、积分多行为/earned/日期传后端；日期采用含边界毫秒，保留 false/0；移除当前页补过滤，以同条件 records/total 分页，保留 URL 与账号隔离；CSV 导出当前筛选页并保留原始业务类型 | Chrome 钱包原 39 条→理财+锁仓桶 2 条→加日期范围 0 条，total/导出条数一致，空结果禁导出；积分原 1 条→CONSUME+RECHARGE 0 条，刷新保留条件。API 测试覆盖 earned=false/startAt=0；非空深分页和本轮实际下载未验 |
| 7 评价资格 | 列表改批量 eligibility、每批最多 200、按 ID 映射；漏项/失败待核对；详情/写评价使用详情 reviewEligibility，展示不可评价原因，不用 reviewId 强开可能已删除详情 | 不再为特定订单扫描待评价分页；乱序、分批、漏项、删除后无资格有测试；真实身份/时间窗口/删除回归未完成 |
| 8 进度/统计 | 本人求购读取 my/progress，展示真实正序节点及推送汇总；买手工作台独立读取 stats/mine，按原百分数/小时展示，不夹断比率；错误、无数据、归属异常分别处理 | Chrome 工作台真实评价率 12.50%、客诉率 33.33%、平均发货 0.87 小时，完成 8/评价 1/售后 4；时间参数为含边界毫秒，工作台沿用全时间范围。范围边界、>100% 及错误场景未验 |

任务 8 补验：Chrome 从本人列表进入已有隔离求购，真实显示提交→取消两个节点、推送 0 批/触达 0；审核/接单/非零推送和非本人边界仍未验。巡检另发现求购两页还残留“仅当前页筛选”旧说明，已改为服务端条件/总数口径，未改变交互。补验待审核筛选返回 0 条，全部/已取消标签各保留服务端 6 条，刷新后待审核条件保持。

已保留而不重做：原下单/押金/IM 幂等、Long ID、账号隔离、现有私有文件 access 授权刷新。不执行 SQL、全量 QA prepare/reset、真实链上转账，不替换文件 URL 主机。AML/CMS/AI 本期延期，页面仅保留必要不可用态和协议演示声明，详见PC问题表。

环境证据：上午 user Swagger/auth/me 404 为历史阻塞；19 时复核 user 已恢复，真实登录和四域契约检查通过，不再列为当前阻塞。9月5日两账号 `/api/user/kyc/detail` 均 code=-1、data=null、消息“文件不存在或已删除”；`/api/user/recharge/address?chain=TRON` code=-1，提示缺少 `chain.callback.base_url`；理财产品当时返回空数组；9月6日错误地址触发详情失败，不能据此判断列表失败，正式入口复验见PC-11c。需后端修复关联影像/私有资源和链服务配置，并提供可申购隔离产品；前端不替换签名 URL 主机或更改共享后台配置。

本轮门禁：新版 `check:swagger` 全量通过，admin 201/202/337、user 50/50/92、order 73/75/113、notify 22/22/35（路径/操作/schema）；增加幂等键类型/长度、by-key 必填键/响应字段、KYC 配置类型及筛选时间类型检查。类型检查、现有 9 文件 184 项测试、生产构建及 diff 检查通过，保留原 Arco 约 583 kB 警告。仅扩展现有测试，没有新增测试文件、框架或依赖。

前序本地 Chrome 已使用文档明确记录的测试账号真实登录 localhost:5173，未使用演示快捷登录。除上述钱包/积分/KYC 回读，还确认充值地址失败时既有 4 条已到账记录保留、提现现有 5 条记录正常及空地址提交禁用、理财显示真实无可申购产品。上午首页/帮助/AML 证据保留。前序没有新增订单/资金/文件/消息写入，不将代码测试计为完整业务验收。C-PC 本地服务保留，未启动本地后台、未操作其他端会话。状态：部分完成；未 commit/push/发布。任务 2/4/6 的 user 分支适配已补齐，后续按各行未验场景准备隔离条件并回归，不再等待 user 注册恢复。

用户提供桌面测试图片后的追加回归（约 19:35–19:49）：

- KYC：已注册独立账号“QA-CPC-认证回归-0905”，在 127.0.0.1:5173 登录并取得可编辑表单，原 localhost 账号认证状态未改。选定桌面测试图片中的无个人信息纯色 PNG（5679 字节）；文件选择控制返回 `Not allowed`，页面未收到文件，不改走隐藏接口上传。缺资料页时提交被前端阻止、测试姓名/证件号草稿保留；未创建认证申请、未上传成功。临时草稿已清空，独立账号登录页恢复到 KYC 并保留，后续从文件选择继续。
- 后台协作：实际打开部署后台。两组文档联调账号可登录，但理财分页均返回 `-202 / 缺少权限: finance:product:list`；文档管理/财务/KYC 演示账号各核对一次均返回“用户名或密码错误”，未继续猜测。无法自主创建隔离产品、审核或改共享链配置；需有效管理测试会话/权限。已查后端说明及项目文档，未取得可验证的充值回调实际服务地址，不凭空填入。
- IM：复用已完成的隔离订单 `2093369377458577408`，未使用其他端正在操作的订单。两账号在 localhost/127.0.0.1 独立登录，仅发送一条本批 QA 文本；接收端尚未进入该会话时会话角标为 1，进入且页面 visible 后总未读 60→59，发送端显示“已读 1”。消息随即撤回，双端均为占位；刷新双端后本批正文不存在。只证明该文本/可见阅读链路，不代替媒体与真正隐藏预取验收。测试消息已撤回，未删除原业务会话。
- 本批没有新增代码、测试文件、订单、资金、物流或文件数据；新增 1 个隔离测试账号和 1 条已撤回文本。实时四域 Swagger 检查及文档 diff 检查通过；184 项测试/构建为前序代码验证，本批不重复执行。未 commit/push/发布。剩余上传受浏览器控制限制，后台写入受文档账号权限/有效性限制；资金异常恢复、发货凭证及媒体回归仍不计完成。

## Mock 与交付边界

- 已接入模块仅用真实 API，不以 Mock 回退掩盖失败；类型、枚举及格式化共享代码不因清理而删除。
- AI、公告、帮助不调用模拟业务；个人中心空公告卡已移除，旧路由仅简短不可用态和安全返回。缺能力原因仅记在 PC 问题表。
- 协议仍为本地演示且明确非正式发布版本；正文、版本、query打开与同意流程保留，不计真实CMS接入。人民币参考折算不改，不计真实汇率服务。
- 开发演示身份限 DEV 且不替代真实 currentUser；测试充值保留测试标记，真实提现到账必须有真实txid。
- 9月5日只读回归保留：钱包39=20+19、筛选/刷新/重置及顺序切账号；积分1→多行为0→重置1；求购取消6/待审核0、提交取消节点；卖出订单第二页4/12及同单详情后退；已评价订单无重复表单；通知16/未读11、已读通知跳同单。无非空深页/新资金或上传验收。
- 9月6日定向回归见PC-03/11c/13：工作台层级、钱包行/抽屉/选项与筛选提交、单行CSV实际下载、积分标签、个人中心公告移除、AI/公告/帮助返回与协议声明；KYC资料读取失败。理财错误详情地址结论已纠正，正式入口自然空态及三个HTTP200已验；另一次同身份独立API只读确认产品空数组及合法零收益/锁仓，非页面响应体捕获；受控异常未验。两日证据不互相替代。
- 浏览器仅Chrome插件本地页面；旧批另有授权Safari历史导航/双账号证据。没有在本批提交整理中新增浏览器或业务写入，不把控制台无错当全站验收。

## 本次提交验证（2026-09-06）

第一批补验已以b6bb4f0提交并推送：理财产品页及两份既有文档，共3个文件；修正分项失败提示、旧收益残留和缺失收益误补零，保留钱包筛选/CSV及理财自然读取证据，异常页面分支仍未验。用户明确要求更新推送后，重新运行生产构建（含类型检查）通过，保留原Arco约583 kB提醒；184项既有测试为第一批代码修正后通过的结果，diff检查通过。推送后本地与远端一致；未提交本机账号文件、下载文件或构建产物，未部署。

本次交付第二批共6个文件：订单详情、卖出订单卡片、订单DTO、既有order-mapper测试及两份既有文档。修正无商品ID订单的无效商品跳转和卖出卡片读屏标签；完成指定订单/已读通知同实体、本人求购进度及原生返回、零未读指定IM群的既有撤回展示定向验证。四域Swagger、类型检查、9文件184项既有测试及diff检查通过；用户明确授权推送后，第二批生产构建（含类型检查）另行运行通过，保留原Arco约583 kB提醒。提交前fetch确认main与origin/main无差异提交，本次按阶段部分验收结果提交推送，实际提交及远端位置以Git为准，不执行手动部署。非零求购推送缺样本；顾客评价资格本轮仅API验证；采购凭证无原展示入口不扩交互；图片预览、新上传/发货/物流异常及IM隐藏预取/新媒体撤回/独立群故障重连仍未完整验收。IM前后未读和水位不变不等同绝对无写，保留可能显式read调用的边界。

第三批3A以已推送第二批7de1a47为基线：本批10文件，为资金保护工具、订单API、订单详情/卡片/动作组件、提现页面、两份既有测试及两份既有文档。损坏记录、坏回执及同单跨动作用例先复现失败；最终类型检查、9文件194项测试（请求子集67→77：4项损坏记录用例、6项同单互斥/终态/存储/切账号用例；押金/退款/合法原回执和0E-8扩原用例）、四域Swagger、生产构建及diff检查通过，保留原Arco 583.19 kB大块提醒。未新增测试文件、依赖或临时脚本。用户明确“更新推送”后按阶段部分完成交付，提交及真实远端位置以Git为准，不执行手动部署，不修改顶部承接状态。

3A安全API只读证据见上表；Chrome本地原QA账号复验提现5条记录、APPROVED/REJECTED两笔详情和原REDEEMED锁仓详情，未执行业务写入。提现2090377523934879745为APPROVED/20，2094235177690161154为REJECTED/20，均无到账时间/链上凭证；契约actualAmount是申请额扣手续费后的金额，三处标签改为“扣费后金额”，不改数值、状态或计算。列表及APPROVED详情刷新后标签已验，新申请回执区域未触发；原已赎回锁仓100、赎回按钮禁用，曲线语义问题保留在PC-11c。该范围控制台无warning/error，自建标签已关闭。APPROVED不是失败出款证据，不重试/审批/调账/真实链上转账。3A已随bcde271推送，工作区当时干净且真实远端同SHA。后续定向订单互斥实施见3B，第二批遗留未验项不因3A检查或推送而关闭。

第三批3B以bcde271为基线，仅定向订单锁小切片，共9个既有文件：资金工具、订单API、checkout、订单详情/卡片、原type/page测试及两份文档。修复和边界见“下单/资金互斥”；保留原下单幂等键、金额确认、逐单结果及部分失败逻辑，不新增框架/文件/依赖/Mock。用户明确“先推送吧”后重新运行最终类型、9文件203项测试（请求77→86）、四域Swagger和生产构建，均通过；diff检查通过，原Arco 583.19 kB提醒保留。测试包括固定顺序/重叠组/无关单/账号隔离、后续锁失败释放、切账号零回调、坏ID/重复ID、付款与退款/收货互斥、回调有效期、历史逐单无重入及部分失败、读取和回查期间持锁。普通组结果对象与确认金额透传沿原page用例验证；不把隔离测试当作实际支付或页面验收。实时/orders/pay仅ApiResultLong，data为int64，无原订单ID语义说明，原有效回执判断暂保留。本批按阶段部分完成提交推送，实际提交和真实远端位置以Git为准；不部署，不改顶部承接状态。尚无本切片浏览器/真实业务写入/跨刷新未知结果验收，未验项继续保留。

前序已交付批次包含86个已跟踪改动：历史契约/API/类型、资金幂等与金额保护、上传/IM/页面适配、已有测试/契约脚本及显示层整改。未新增测试文件、依赖或仓库临时文件。类型检查、9文件184项测试、四域Swagger和生产构建通过，保留原Arco约583 kB大块提醒；git diff --check通过。提交前main与fetch后的origin/main无差异提交；本批按部分完成阶段交付，实际提交及远端位置以Git为准，不执行发布。未修改顶部跨设备承接状态、AGENTS或README。

文档清理仅合并既有两份文档的重复日期流水账、旧“缺接口/等待恢复”结论及已被后续覆盖的检查次数；保留本表、接入结果、最新有效验证与未验分支。没有删除独立文件，旧已提交记录可由Git历史追溯。

## 接口接入原则

1. 修改接口前刷新实时 Swagger，并按“页面交互 -> 现有数据/Mock -> Schema”核对需求。
2. 接口路径、方法、字段、类型、枚举、分页和响应结构以当前有效契约为准。
3. 字段差异在 `src/service/api/` 或 adapter 边界转换，不把后端 DTO 扩散到页面。
4. 所有业务 ID 作为不透明 Long 原值处理；禁止页面层使用 `Number()`、`parseInt()` 或算术转换。
5. 分开记录“Swagger 存在、API 已封装、页面已调用、真实数据已验证”，不得相互替代。
6. 真实接口失败不得回退 Mock；空数据、权限失败、后端异常和环境阻塞应明确展示。
7. 页面交互改动使用 Chrome 检查页面、接口响应、控制台和数据回显；不把无控制台错误等同于业务闭环完成。

## 跨设备承接

1. 执行 `git status -sb`；工作区干净时运行 `git fetch origin` 和 `git pull --ff-only origin main`。
2. 读取本文顶部、[AGENTS.md](../AGENTS.md) 和 [Swagger 匹配矩阵](./api-swagger-match-matrix.md)。
3. 仅在依赖缺失或锁文件变化时运行 `pnpm install`，优先复用已有 `5173` 服务和 Chrome 登录态。
4. 涉及接口时运行 `pnpm check:swagger`；TypeScript/公共逻辑变化运行 `pnpm typecheck`；业务逻辑变化运行 `pnpm test`。
5. 押金、积分、VIP 已完成最近 Chrome 读取复验；其余事项等待上述后端/环境条件后从对应恢复点继续。

历史测试 ID、逐次回归过程、旧 Swagger 数量和旧进度百分比不再写入本文；需要追溯时使用 `git log -- docs/api-integration-plan.md` 和 `git show <commit>`。
