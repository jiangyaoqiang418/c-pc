# C-PC Codex 开发交接（2026-08-15）

> 用途：在新的 Codex 任务中快速恢复项目上下文。开始工作前依次阅读本文件、`AGENTS.md`、`docs/api-integration-plan.md` 和 `docs/api-swagger-match-matrix.md`。本文件记录的是最新运行时状态；若与两份历史长文档中的旧结论冲突，以本文件和实时接口复验为准。

## 1. 项目与 Git 状态

- 项目：油宝 C 端 PC，Vue 3 + Vite + TypeScript + Arco Design Vue + Pinia + Vue Router。
- 本机路径：`/Users/yaoqiangjiang/Desktop/w3/c-pc`。
- GitHub：`git@github.com:jiangyaoqiang418/c-pc.git`。
- 分支：`main`。
- 当前本地和远端最新提交以 `git log -1 --oneline` 为准。
- 上一个关键提交：`c955fa3 fix: 完善交易与钱包异常态`。
- 2026-08-15 推送后，本地与 `origin/main` 同步。
- 当前仅有未跟踪临时目录 `tmp/`，其中是本地 QA 图片，不属于项目代码，默认不要提交。

### 用户约定

- 默认使用用户现有的 Chrome 会话做页面联调。
- 用户已明确授权自主提交与推送：每批功能完成并通过适用验证后可自行提交、推送；构建失败时不得推送。
- 用户授权在既定范围内自主推进：正常代码修改、验证、文档同步、提交和推送不逐项确认；仅在必要条件缺失、会扩大任务范围或受平台安全规则限制时暂停说明。
- 不清理现有测试数据、商品、订单、充值记录、通知或会话。
- 工作区可能存在用户自己的改动；只提交本次任务涉及的文件。
- Long / BigDecimal 均按字符串处理，不可用 `Number()`、`parseInt()` 处理业务 ID。

## 2. 启动与质量门禁

```bash
cd /Users/yaoqiangjiang/Desktop/w3/c-pc
pnpm install --frozen-lockfile
pnpm dev --host 0.0.0.0
```

- 本地地址：`http://localhost:5173/`。
- 当前测试环境代理统一指向 `https://testhou.merchantsale.store/api/*`。
- 生产构建：`pnpm build`。
- 单元测试：`pnpm test`。
- Swagger 契约：`pnpm check:swagger`。
- 格式检查：`git diff --check`。

最近一次完整结果：

- `pnpm build` 通过；只有已有的大 chunk 体积警告。
- `pnpm test`：3 个文件、10 项用例全部通过。
- `pnpm check:swagger`：通过。
  - admin：116 paths / 117 operations / 207 schemas
  - user：33 / 33 / 66
  - order：45 / 47 / 66
  - notify：16 / 16 / 22

## 3. 最新整体进度估算

以下百分比是基于页面接入、真实写读回归和当前后端契约的工作估算，不是单纯的 Swagger 路径占比：

| 梯队 | 当前完成度 | 状态 |
|---|---:|---|
| 主交易链（商品、购物车、结算、支付、发货、签收） | 约 95% | 双账号真实闭环通过 |
| 仅退款售后 | 约 95% | 申请、撤销、后台驳回、后台同意、资金解冻、库存回补均通过 |
| 求购链路 | 约 90% | 创建、抢单、支付、发货、签收真实通过 |
| 钱包、充值、提现、押金 | 约 85% | 真实接口和主要非空回显完成；链上实际到账与提现上链仍需环境回归 |
| IM 与站内通知 | 约 85% | REST 写读和历史回读正常；WebSocket 实时推送仍受阻 |
| KYC 与买手申请 | 约 90% | C 端提交、后台审核、前台状态回读通过 |
| Mock 专题（AI、理财部分流程、评价等） | 约 45% | 缺对应 C 端真实契约或稳定测试数据 |
| 整个 C-PC | 约 80% | 核心业务可用，剩余集中在实时推送、Mock 专题和自动化环境 |

## 4. 已完成的核心真实闭环

### 账号与基础能力

- 邮箱注册、邮箱密码登录、当前用户、退出登录均接真实接口。
- 当前用户角色、KYC、VIP、积分、钱包总览均真实读取。
- 普通顾客不显示不可用的买手身份切换；具备 `BUYER` 角色才显示买手入口。
- 购物车按用户 ID 隔离缓存，切换账号不会读取其他账号购物车。

### 商品与交易

- 买手商品图片上传、创建、后台审核、公开展示、详情、收藏、上下架均已接入。
- 自购商品前端预拦截和后端业务错误兼容已完成。
- 跨账号 `create-batch → group/pay → ship → confirm` 完整通过。
- 订单列表、详情、物流公司、运单号、发货/签收时间、支付业务号和退款概要均已映射。
- 无真实物流轨迹时只展示后端可证明的发货与签收事件，不生成虚假清关轨迹。

### 售后

- 仅退款使用最新接口：创建、买家分页、详情、撤销。
- 已真实验证：买家申请、买家撤销、后台驳回、后台同意。
- 后台同意后钱包冻结解除、退款流水生成、商品库存回补、订单进入 `REFUNDED`。
- 卖家侧售后为只读，平台后台负责审核。

### 求购与买手

- 求购创建、我的求购、求购大厅、详情、取消、抢单已真实接入。
- 求购抢单生成关联订单后的支付、发货、签收闭环已通过。
- 买手申请提交、后台“买手管理 → 申请审核”、C 端角色生效已通过。
- 买手保证金缴纳、退还和双向流水已通过。

### 前端体验和异常态

- 登录/注册 Arco 组件注册和 placeholder 已修复。
- 购物车结算栏溢出、摘要垂直居中、订单详情居中与内容宽度已修复。
- 全站 `.page-title` margin 和主要页面顶部间距已收口。
- 订单、售后、钱包、买手页面补齐失败提示、重试入口、防重复提交和窄屏布局。
- 浏览器 favicon 已加入 `public/favicon.svg`，入口在 `index.html`。

## 5. 充值页最新状态（提交 c5ee338）

- `GET /user/recharge/chains` 已真实返回 TRON：
  - `label=波场`
  - `decimals=6`
  - `minAmount=10`
  - `enabled=true`
  - `depositAddress=TQ5NMqJjW3jGuNqk4rBnkqmGkTkT7VtQyz`
- `/wallet/deposit` 现在选择链后直接展示专属充值地址、最低金额和复制按钮。
- 用户无需先创建充值订单即可向专属地址转账。
- `POST /user/recharge/create` 仍保留为可选“充值申报记录”，不再作为获取地址的前置条件。
- 充值记录/详情仍接 `page/detail`；当前有已到账记录 `2086475961462251521`。

## 6. IM 与通知最新事实（重要）

### 已恢复并验证

- Notify Swagger 已生效：16 paths / 16 operations / 22 schemas。
- `/im` 实时读取到 11 个订单三方群。
- `POST /im/conversations/page`、`POST /im/messages/page`、`GET /im/conversations/by-order` 正常。
- 旧文档中“刷新后消息分页为空”的结论已经过期：2026-08-14 整页刷新后，会话列表和选中会话历史仍能完整回读。
- 历史订单群 `2086788120608071682` 可读取订单卡片、系统消息、文本、图片、撤回记录和输入区。
- 文本与图片真实发送、双方刷新回读、撤回已通过。
- 消息列表按 Long ID 正序，最新消息在底部；输入框不在滚动区域内。
- 发送、增量同步和选中会话后自动滚动到底部；加载旧消息保持原阅读位置。
- 图片点击使用 Arco 预览组，同一会话多张图片可翻页。
- 独立订单群右上角有关闭按钮，返回 `/im` 后恢复当前会话。
- 站内通知页 `/notifications` 正常，2026-08-14 实测 10 条通知、9 条未读；列表、分类、已读和删除能力可用。

### 当前唯一 P0：WebSocket 仍未生效

- C-PC 已完成 WebSocket URL、`ws: true` 代理、PING/PONG、断线退避、去重、已读、撤回和增量补偿。
- 页面仍会显示“实时连接暂不可用”，重连后再次关闭。
- 2026-08-14 直接探测以下两个地址，HTTP Upgrade 都返回 `200 OK`，不是 `101 Switching Protocols`：
  - `wss://testc.merchantsale.store/api/notify/im?token=...`
  - `wss://testhou.merchantsale.store/api/notify/im?token=...`
- 因此 REST 发送和刷新回读可用，但双方保持页面打开时不能证明实时下推。
- 后端/Nginx 修复后必须完成：双账号同时在线 → A 发送 → B 无刷新收到 → PING/PONG 维持 → 已读/撤回实时同步 → 断网恢复后增量补偿。

### 次要后端字段缺口

- 历史通知的 `bizType/bizId/templateCode` 可能为 `null`。前端会安全停留在通知列表，不猜测订单。
- 新通知需要真实返回这些字段，才能完成“点击通知跳订单”的运行时闭环。

## 7. 可复用账号与测试数据

> 不把密码写入仓库。优先使用用户 Chrome 中现有登录态；登录态失效时再向用户确认凭据。不要清理以下数据。

| 账号/昵称 | 已知标识 | 角色与用途 |
|---|---|---|
| `john` | userId `2082303088212398081` | 顾客 + 买手；有商品、卖出订单和历史 IM，用于与 mamba 双向交易 |
| `mamba` | 邮箱 `jiangyaoqiang@gmail.com`；userId `2086359209189400577` | 顾客 + 买手；钱包余额充足，有订单、求购、商品、退款、充值和通知数据 |
| `QA IM 回归` | userId `2087121982819225601` | 后台审核通过的 QA 买手；用于独立账号、商品、订单及 IM 回归 |
| QA KYC | `qa.kyc.20260810.2019@example.com` | 使用带 QA 水印的非真人模拟资料完成 KYC 提交和后台审核 |

关键保留数据：

- 商品 `2086766319630901249`：`QA 耳机盒跨账号回归 20260810`。
- 商品 `2087163647210643458`：`QA 隔离买手商品 20260811`。
- 完整跨账号订单 ID `2086788120608071682`，订单号 `2086788120595488768`。
- 仅退款订单 ID `2087045217757257730`，订单号 `2087045217635622912`。
- QA 隔离订单号 `2087164523669184512`。
- 完整求购履约订单 ID `2086783649773608962`。

后台入口：

- 本地后台曾使用 `http://localhost:9527/`。
- 测试后台使用 `https://testhou.merchantsale.store/`，优先复用 Chrome 中 `super` 登录态。
- 买手申请审核入口在“买手管理”，不是“审核中心”。
- 仅退款审核入口在“售后仲裁”。

## 8. 仍可推进与受阻事项

### P0：等待后端/网关

1. 修复 Notify WebSocket Upgrade，必须返回 `101 Switching Protocols`。
2. 修复后完成双账号实时消息、PONG、实时已读/撤回和断线增量回归。

### P1：前端可继续做

1. 真实语音消息验收：Chrome 麦克风授权、WebM 上传、时长、双方播放、失败重试。
2. 给 IM 关键交互补浏览器级自动化：发送后到底部、历史分页保持位置、关闭独立会话回定位、图片翻页。
3. 全站 1366px 和窄屏静态巡检，重点检查长订单号、长地址、按钮组和详情表格。

### P2：依赖后台测试数据

1. 配置启用的秒杀场次和符合条件的在售商品，完成真实报名与状态回显。
2. 给已完成订单补采购凭证、发货凭证非空数据，回归详情展示。
3. 给新通知补 `bizType/bizId/templateCode`，回归点击跳转订单。
4. 提供可重置的双账号、余额、商品、订单夹具后，再建立稳定 E2E 主交易回归。

### P3：暂时仍为 Mock 的专题

- AI 导购。

## 12. 2026-08-17 理财、评价与资金/通知契约接入

### 已接入（代码与最新 Swagger 校验通过，未等同于浏览器真实闭环）

- 理财：产品列表/详情、申购、锁仓订单分页/详情、提前赎回已改用 `/user/finance/**`；年化利率按小数 `annualRate`、锁定期按 `lockDays` 映射，订单状态覆盖 `HOLDING/REDEEMED/SETTLED/CANCELED`。
- 评价：待评价、我的评价、买手收到评价、商品评价分页/评分摘要/买手评分，以及提交、回复、申诉均改用 `/order/reviews/**`；评价图片走真实 `files/upload?dir=review`。
- 充值：页面已通过 `/user/recharge/address?chain=` 获取用户专属地址、Memo、最低金额和确认数，不再把创建充值申报单当成收款地址前置条件。
- 通知：资金、KYC、买手申请、理财、商品评价的 `bizType` 已有安全跳转；字段为空时保留在通知列表，不猜测业务对象。
- WebSocket：前端按后端新协议使用 25 秒心跳，收到 `READY` 才标记连接成功，10 秒未收到 `READY` 会关闭后按退避策略重连。

### 已验证与待验证边界

- 已执行：`pnpm typecheck`、`pnpm test`（5 文件、14 项）、`pnpm check:swagger`、`pnpm build`、`git diff --check`，均通过；构建只有既有 Arco 大包警告。
- Chrome 当前未连接到可控会话，因此理财申购/赎回、评价图片上传/提交/评分回读，以及双账号 IM 实时到达尚未标记为浏览器真实回归通过。
- 2026-08-17 以有效 C 端 token 实测：理财产品/订单、我的评价、待评价接口均返回 `code=1`；Notify 自检接口正常，但 WebSocket Upgrade 返回 nginx `400 Bad Request`，不是 `101`。WebSocket 验收仍要求有效 token 握手返回 `101`、首帧 `READY`、PING/PONG 和双账号无刷新送达；未修复前禁止以前端本地消息冒充成功。
- 后端修复说明列出的提现 `payoutId/payoutStatus/dispatchedAt/submittedAt/blockHeight/networkFee/networkFeeSymbol`，尚未出现在本次实时 user Swagger 的 `WithdrawVO`；前端暂不猜测字段，待 Swagger 发布后再补详情展示。

## 9. 新 Codex 接手后的推荐首轮动作

1. `git status -sb && git pull --ff-only`，确认最新提交为 `c5ee338`，保留 `tmp/`。
2. 阅读 `AGENTS.md` 和本文件。
3. 运行 `pnpm install --frozen-lockfile`，再启动 `pnpm dev --host 0.0.0.0`。
4. 运行 `pnpm check:swagger`，确认四组文档数量和关键契约没有变化。
5. Chrome 登录 `mamba`，检查：
   - `/wallet/deposit` 是否显示 TRON 专属地址与最低金额；
   - `/im` 是否仍有 11 个会话、刷新后历史是否保留；
   - `/notifications` 是否能读取真实通知；
   - 页面是否仍提示实时连接不可用。
6. 使用 Upgrade 探测确认 WebSocket 是否从 `200` 变为 `101`；只有变成 `101` 后才开始双账号实时回归。
7. 开发完成后运行 `pnpm test`、`pnpm check:swagger`、`pnpm build`、`git diff --check`。
8. 用户未明确说“推送”时，不提交和推送。

## 10. 关键代码位置

- 请求层：`src/service/request/`
- 真实 API：`src/service/api/`
- 真实类型：`src/typings/api/`
- 充值页：`src/views/wallet/deposit.vue`
- 钱包 API：`src/service/api/wallet.ts`
- IM API：`src/service/api/notify.ts`
- WebSocket Store：`src/stores/notify.ts`
- 消息中心：`src/views/im/index.vue`
- 独立订单群：`src/views/im/order-group.vue`
- 消息输入：`src/components/im/message-input.vue`
- 消息气泡：`src/components/im/message-bubble.vue`
- 通知中心：`src/views/notification/index.vue`
- IM 工具与测试：`src/utils/im.ts`、`src/utils/im.test.ts`
- 通知工具与测试：`src/utils/notification.ts`、`src/utils/notification.test.ts`
- Swagger 校验：`scripts/check-swagger-contract.mjs`
- 历史接入计划：`docs/api-integration-plan.md`
- 历史匹配矩阵：`docs/api-swagger-match-matrix.md`

## 11. 2026-08-15 浏览器复核

- 受控测试账号登录后，`/im` 成功读取 11 个订单三方群、历史消息和未读数；Notify REST 读链路正常。
- 点击“立即重连”后仍显示“实时连接暂不可用”。携带有效测试登录态的 WebSocket Upgrade 探测未返回 `101 Switching Protocols`，网关返回 `400 Bad Request`；此项继续作为 Notify 网关/后端阻塞，不能标记为实时下推通过。
- `/buyer/flash-sales` 正常渲染，但实时接口返回可报名场次 `0`、我的报名 `0`、可用在售商品 `0`，秒杀写入回归仍缺测试数据。
- 已完成订单详情可回显订单状态、物流、收货地址、金额、支付凭证和关键时间；采购/发货凭证仍由后端返回为空，前端已明确降级。
- `/notifications` 当前可读取 10 条真实通知。以上均为只读复核，未发送消息、修改通知或产生订单/资金写入。
- `src/main.ts` 已移除将 Arco `ImagePreviewGroup` 当作 Vue 插件注册的错误写法；`pnpm typecheck`、`pnpm test`（10 项）和 `git diff --check` 均通过。
- 已在用户确认后对本人待到账申报单 `2086339526209921026` 调用测试专用模拟到账接口：响应 `testData=true`，申报单状态变为 `CONFIRMED`，详情生成测试交易哈希与确认时间；充值页和资金流水均回读到对应的测试到账记录。该接口写入了测试账号钱包和流水，不可作为真实链上到账结论。
- 此次到账后通知分页总数仍为 10，未产生充值通知；确认“充值单 → 钱包 → 流水”通过，“到账 → 通知”仍待 Notify 后端事件补发。
- 钱包流水已识别 `CHAIN_DEPOSIT` 与测试备注中的 `DEV-TEST-*` 哈希；列表和详情统一显示“测试模拟到账”，不再误标为“人工调增”。
- 消息中心和通知中心的读取失败不再静默展示为空数据：会话、消息或通知加载异常时均明确说明错误并提供就地重试；消息中心会保留“请求失败”和“暂时没有会话”的语义区分。
- Notify WebSocket 未恢复前，`/im` 已提供 REST 降级刷新：会话列表刷新、当前会话增量同步、断线提示“刷新数据”均会重新读取会话并同步当前消息，同时显示最近同步时间。Chrome 已在 11 个真实订单群下验证三处入口；未发送消息。
- 消息中心初始加载期间侧栏会明确显示“正在同步会话”，不再把等待真实接口返回的短暂状态误显示为“暂无三方群”。
- 资金流水支持按当前已加载筛选结果在浏览器本地导出 CSV，不依赖后端导出接口；导出包含测试数据标记和解析后的交易哈希，使用 UTF-8 BOM 保证中文表头在常见表格工具中正常打开。
- 通知跳转仅在后端明确返回 `bizType=ORDER` 和 `bizId` 时进入订单详情；字段缺失或未知业务类型会保留在列表并提示原因，不再静默无跳转或猜测目标。
- 积分申诉已从浏览器原生 `prompt` 改为页面内表单弹窗：支持 10 至 500 字原因校验、提交中保护，成功后刷新流水与申诉记录；真实提交仍需有可申诉积分流水时另行回归。
- 秒杀报名页的场次、我的报名和可报名商品任一真实读取失败时，现会明确显示“秒杀数据加载失败”与就地重试；报名、取消失败则保留当前上下文，不把请求异常误展示为无数据或未处理回调。
- IM 的“加载更早消息”、删除会话和 REST 同步在请求失败时均保留当前消息、会话与最近成功同步时间；请求层提示错误后可直接重试，不会把失败写成成功状态。
- 通知页的全部已读、单条删除和清空通知在真实接口失败时不会先修改本地未读数或列表；全部已读和清空操作增加提交中保护，避免重复写入。
- 分类申请页的真实列表读取失败会显示可重试错误态；上级分类树读取失败时仍允许提交顶级分类申请，提交失败保留表单内容供重试。
- 买手售后与可接求购列表均已区分请求失败和真实空数据；求购接单增加进行中保护，失败时保留当前记录以便重试。
- 真实收藏列表读取失败会显示重试入口；取消收藏失败不修改当前列表或分页，避免后端未写入时出现本地假成功。
- 公开商品列表的真实分页读取失败会明确显示重试状态；路由中的价格与页码参数改为有限数值校验，异常 query 不会传入接口或造成非法分页。
- 分类页的真实分类树和分类商品读取均已区分失败与空数据，支持分别重试；顶部分类导航的挂载期分类读取失败会安全收起，不再产生未处理异常。
- 地址管理与结算地址选择器均已区分地址读取失败和空地址；新增、编辑、设默认、删除失败会保留当前地址或表单，默认/删除操作增加进行中保护，选择器按 Long 字符串值比较避免 ID 类型不一致。
- 我的求购和求购详情均已区分真实读取失败与空数据；撤销、接单增加进行中保护，失败时保留当前请求；详情页的“我的求购”身份判断按 Long 字符串值比较。
- 求购创建页的真实分类树加载失败会明确提示并提供重试；创建请求失败保留完整表单内容，不会产生未处理确认回调或清空用户输入。
- 买手订单的真实列表读取失败会明确显示重试；改价和发货失败保留对应弹窗与表单，避免写入失败后出现本地假成功。
- 买手商品创建的真实分类树加载失败会明确提示并可重试；创建提交失败保留商品表单和上传结果；商品上下架增加进行中保护，避免并发重复写入。
- 2026-08-15 推送后 Chrome 只读复核：已打开的 `/im` 页面可读取 12 个订单三方群、当前会话历史与未读数，控制台无 warning/error；仍显示“实时连接暂不可用”。直接页面跳转后本地开发会话未持久化而回到登录页，未输入凭据、未执行任何写操作。
- 会话初始化已修正：`/auth/me` 的临时网络失败、HTTP 500 或普通业务失败不会再清除本地 token 并降级到 Mock；仅 HTTP 401/403 或业务码 `-200/-201` 才清理登录态。新增 2 项单测覆盖身份失效与非身份异常判定。
- 登录页的真实账号登录和演示账号登录失败均已捕获：请求层提示错误后保留当前登录页和输入内容，避免当前用户读取失败时产生未处理回调或错误跳转。
- 本轮最终质量门禁：`pnpm typecheck`、`pnpm test`（4 个文件、12 项）和 `pnpm build` 均通过；生产构建仅保留既有的 Arco 大包体积警告。
- 2026-08-15 Chrome 真实回归：测试账号刷新 `/wallet/history` 后仍保持登录态；直达 `/im`、`/notifications`、`/wallet/history` 均停留在受保护页面，没有因普通请求异常被清理 token 或跳回登录页。
- IM 语音真实链路已回归：Chrome 麦克风授权后可开始及停止录音，录音成功上传为 WebM 并在当前会话生成带原生播放器的语音消息；播放器元数据正常加载，键盘触发播放后播放时间正常推进。该回归产生了一条测试语音消息；Notify WebSocket 仍未恢复，实时下推问题不受此结论影响。
- IM 发送中的互斥保护已补齐：父页面的真实发送请求未完成时，文本、图片和语音入口都会禁用，避免一次请求尚未返回时产生重复消息；失败后会恢复入口并保留既有失败消息重试语义。
- 独立订单会话复用同一互斥规则，主消息中心与独立会话入口不会再出现重复发送保护不一致。
